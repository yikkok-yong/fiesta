{
  "name": "rn-helper",
  "version": "0.1.0",
  "description": "a general helper for react-native",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && npm install -g",
    "build": "babel src -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react-native",
    "toolbox"
  ],
  "author": "Yik Kok",
  "license": "MIT",
  "bin": {
    "generate": "./dist/index.js"
  },
  "dependencies": {
    "commander": "^2.17.1",
    "shelljs": "^0.8.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.1",
    "@babel/preset-env": "^7.0.0"
  }
}
